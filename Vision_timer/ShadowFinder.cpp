//Arthur van der Weiden

#include "ShadowFinder.h"
ShadowFinder::ShadowFinder(std::string filename) : filename(filename)  {

	ShadowFinder::img = new Image(filename);
}

bool ShadowFinder::ShadowTest(Image& img){
	bool found = false;




	return found;
}